class Solution(object):
    def minPartitions(self, n):
        """
        :type n: str
        :rtype: int
        """

        # for element in list(n):
        #     int(element)
        # return max(n)
            
        digits = set(n)
        return max(digits)